export class Product {
  filter(arg0: (product: Product) => boolean): Product {
    throw new Error('Method not implemented.');
  }
    public name: string;
    public saltCompostion: string;
    public about: string;
    public use: string;
    public sideEffects: string;
 // manufacturer: any;
}